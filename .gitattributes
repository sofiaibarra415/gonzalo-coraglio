# Auto detect text files and perform LF normalization
* text=auto

# CV en Git normal (sin LFS): Vercel/GitHub Pages sirven el binario; LFS dejaba ~130 B y el PDF “rompía”.
# Si más adelante subís PDFs enormes a public/, podés volver a filtrar solo esas rutas con LFS.
